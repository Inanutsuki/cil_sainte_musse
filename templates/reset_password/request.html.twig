{% extends 'base.html.twig' %}

{% block title %}Modifier votre mot de passe
{% endblock %}

{% block body %}
	{% for flashError in app.flashes('reset_password_error') %}
		<div class="alert alert-danger" role="alert">{{ flashError }}</div>
	{% endfor %}
	<h1>Changer votre mot de passe</h1>

	<div class="container-fluid border-shadow mx-3 py-3">
		{{ form_start(requestForm) }}
		{{ form_row(requestForm.email) }}
		<div>
			<small>
				Entrer votre email et vous reçevrez un lien pour modifier votre mot de passe.
			</small>
		</div>

		<button class="btn btn-primary mt-3">Evoyer l'email de modification de mot de passe</button>
		{{ form_end(requestForm) }}
	</div>
{% endblock %}
