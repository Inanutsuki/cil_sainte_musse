{% extends 'base.html.twig' %}

{% block title %}Editer l'article{% endblock %}

{% block body %}
    <h1>Editer l'article</h1>

    {{ include('post/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary"  href="{{ path('my_posts', {'id': post.author.id}) }}">Retourner à la liste</a>

    {{ include('post/_delete_form.html.twig') }}
{% endblock %}
